# Development Dockerfile for Comics AI Backend (with hot reload)
FROM node:22-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install ALL dependencies (including dev dependencies for tsx)
RUN npm ci && npm cache clean --force

# Copy source code (will be overridden by volume mounts in development)
COPY . .

# Create logs directory
RUN mkdir -p logs

# Note: Source code will be mounted as volumes for hot reload

# Expose port
EXPOSE 3000

# Health check
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
  CMD node -e "require('http').get('http://localhost:3000/health/live', (res) => { process.exit(res.statusCode === 200 ? 0 : 1) })"

# Start development server with hot reload
CMD ["npm", "run", "dev"] 
